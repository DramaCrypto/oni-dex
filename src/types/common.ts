export interface INetwork {
  icon?: string;
  name: string;
  selected?: boolean
}

export interface ITokenForm {
  name: string;
  value: boolean
}

export interface IToken {
  name: string;
  subTitle: string;
  icon: string;
  icon2?: string;
  description: string;
  selected?: boolean
}